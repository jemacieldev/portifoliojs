{
  "compilerOptions": {
    "target": "es2016",                             /* Define a versão do JavaScript para o código emitido. */
    "module": "commonjs",                           /* Define o sistema de módulos a ser usado. */
    "jsx": "react-jsx",                             /* Ativa o suporte ao JSX (use 'react' para React 16 ou anterior). */
    "strict": true,                                 /* Ativa todas as verificações estritas do TypeScript. */
    "esModuleInterop": true,                        /* Permite a compatibilidade com módulos CommonJS. */
    "forceConsistentCasingInFileNames": true,       /* Garante consistência na capitalização de nomes de arquivos. */
    "skipLibCheck": true                            /* Ignora a verificação de arquivos de definição de tipo para melhorar o desempenho. */
  },
  "include": ["src/**/*"],                          /* Especifica os arquivos que devem ser incluídos na compilação. */
  "exclude": ["node_modules"]                       /* Exclui a pasta node_modules da compilação. */
}
